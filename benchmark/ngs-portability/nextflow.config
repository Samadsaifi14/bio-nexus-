process {
  executor = 'local'
  cpus = 1
  memory = '1 GB'
}

timeline.enabled = true
timeline.file = "${params.outdir}/timeline.html"
report.enabled = true
report.file = "${params.outdir}/report.html"
trace.enabled = true
trace.file = "${params.outdir}/trace.txt"
dag.enabled = true
dag.file = "${params.outdir}/dag.html"
